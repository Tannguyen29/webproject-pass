<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Student Management System!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            <!-- CSS only -->
                <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
                
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        <style>
        body {
            padding-top: 100px;
        }
        </style>

        {% block javascripts %}
        			<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-A3rJD856KowSb7dwlZdYEkO39Gagi7vIsF0jrRAoQmDKKtQBHUuLZ9AsSv4jD4Xa" crossorigin="anonymous"></script>
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        {% block header %}

				
				{# bs5-nav-ul #}
				{% if is_granted('ROLE_ADMIN') %}
					<div class="example-wrapper">
    <nav class="navbar fixed-top navbar-expand-sm navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand mb-0 h1" href="#">
                <img class="d-inline-block align-top"
                src="https://greenwich.edu.vn/wp-content/uploads/2022/05/2022-Greenwich-Eng.png" width="150" height="70" />
             </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-link active" aria-current="page" href="{{path('app_admin')}}">Home</a>
                    <a class="nav-link" href="#">CRUD student</a>
                    <a class="nav-link" href="#">Curriculum</a>
                    <a class="nav-link" href="#">Feedback</a>
					<a class="nav-link text-danger" href="{{ path('app_logout')}}">Logout
								<u>
									<i>
										{% if app.user != null %}
											({{ app.user.email }})
										{% endif %}
									</i>
								</u>
							</a>
                </div>
            </div>
        </div>  
    </nav>
</div>
				{% elseif is_granted('ROLE_USER') %}
					<!-- ROLE_CUSTOMER -->
					<div class="example-wrapper">
    <nav class="navbar fixed-top navbar-expand-sm navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand md-0 h1" href="#">
                <img class="d-inline-block align-top"
                src="https://greenwich.edu.vn/wp-content/uploads/2022/05/2022-Greenwich-Eng.png" width="150" height="70" />
             </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav">
                    <a class="nav-link active" aria-current="page" href="{{path('app_student')}}">Home</a>
                    <a class="nav-link" href="{{path('acc_detail')}}">Account Information</a>
                    <a class="nav-link" href="{{path('student_curriculum')}}">Curriculum</a>
                    <a class="nav-link" href="{{path('student_schedule')}}">Schedule</a>
					<a class="nav-link text-danger" href="{{ path('app_logout')}}">Logout
								<u>
									<i>
										{% if app.user != null %}
											({{ app.user.email }})
										{% endif %}
									</i>
								</u>
							</a>
                    
                </div>
            </div>
        </div>  
    </nav>
</div>
				{% endif %}

			</div>
			{# bs5-modal-default #}

			<!-- Modal -->
			

		{% endblock %}
		{% block body %}<div class="container col-md-8 text-center mt-3">{% endblock %}


	</body>
</html>